<% provide(:title, "#{@page.title}") %>
<h1><%= @page.title %></h1>

<div class = "category-display">
    <h5><%= @page.tag %></h5>
</div>

<div class = "body">
    <%= @page.content %>
</div>

<div class = "footer">
    <%= link_to "記事一覧に戻る", pages_index_path %>
    <%= link_to "トップページ", root_path %>
</div>
